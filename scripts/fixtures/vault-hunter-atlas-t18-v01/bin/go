#!/usr/bin/env python3
import json
import os
import sys

with open(os.environ["T18_V01_COMMAND_LOG"], "a", encoding="utf-8") as handle:
    handle.write(json.dumps({"tool": "go", "argv": sys.argv[1:]}) + "\n")
print("go must not be invoked", file=sys.stderr)
sys.exit(99)
