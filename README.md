
Run command below and all should be good.
You might have to remove your own .vim folder so be warned

mv .* ~/

After that be sure to import the .terminal colorscheme and use that as well

Features:

Custom vim
Custom tmux with leader key mapped to C-a
Simple script (dev) to be run on it's own to set up my deafult env
