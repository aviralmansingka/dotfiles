{
  "projects": {
    "mode": "single",
    "root": ".",
    "list": [
      {
        "name": "dotfiles",
        "path": "~/dotfiles"
      },
      {
        "name": "modal",
        "path": "~/worktrees/modal/main"
      }
    ]
  },
  "plugins": {
    "workspace": {
      "dirPrefix": false,
      "defaultAgentType": "codex",
      "startupScript": "~/.config/sidecar/start-codex.sh",
      "agentStart": {
        "codex": "codex",
        "claude": "claude"
      }
    }
  },
  "prompts": [
    {
      "name": "Begin Work on Ticket",
      "ticketMode": "required",
      "body": "Continue work on {{ticket}} in the current Sidecar workspace. The worktree is already created and the td task may already be started. Use td to track progress, but do not run `td start {{ticket}}` again unless you first confirm it is not already in progress."
    },
    {
      "name": "Code Review Ticket",
      "ticketMode": "required",
      "body": "Do a detailed code review of {{ticket}}. Focus on correctness and tests."
    },
    {
      "name": "Plan to Epic (No Impl)",
      "ticketMode": "none",
      "body": "Plan this task into an epic with sub-tasks using td. Do not implement yet."
    },
    {
      "name": "Plan to Epic + Implement",
      "ticketMode": "none",
      "body": "Plan this task into an epic with sub-tasks using td, then implement them."
    },
    {
      "name": "TD Review Session",
      "ticketMode": "none",
      "body": "Start a td review session. Review open tasks, fix obvious bugs immediately, create tasks for larger issues.\n\nUse: td usage --new-session"
    }
  ]
}
