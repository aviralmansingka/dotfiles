# Global markdownlint configuration
# Symlinked to ~/.markdownlint-cli2.yaml via stow

customRules:
  - "./.markdownlint-rules/visual-line-length.cjs"

config:
  # AV001 - Conceal-aware line length
  # Link destinations do not occupy visible columns.
  AV001:
    line_length: 120

  # Replaced by AV001 so formatting and diagnostics share one calculation.
  MD013: false

  # MD033 - Inline HTML
  # Allow HTML in markdown (useful for details/summary, etc.)
  MD033: false

  # MD041 - First line should be a heading
  # Disabled: Files may start with frontmatter
  MD041: false

  # MD024 - Multiple headings with same content
  # Allow duplicate headings in different sections
  MD024:
    siblings_only: true

  # MD026 - Trailing punctuation in heading
  # Allow question marks in headings
  MD026:
    punctuation: ".,;:!"

  # MD036 - Emphasis used instead of heading
  # Disabled: Sometimes emphasis is intentional
  MD036: false

  # MD046 - Code block style
  # Prefer fenced code blocks
  MD046:
    style: fenced

  # MD048 - Code fence style
  # Use backticks for fences
  MD048:
    style: backtick

  # MD049 - Emphasis style
  # Use asterisks for emphasis
  MD049:
    style: asterisk

  # MD050 - Strong style
  # Use asterisks for strong
  MD050:
    style: asterisk
